node_modules
src-tauri/target
dist
dist-linux
.git
*.log
.claude
.icon-hash
.assoc-icon-hash
nul
*.sw?
*.swo
.vscode
.idea

# Updater signing keys — must NEVER enter the build image (the key is injected
# at build time via a BuildKit --secret instead; see scripts/build-linux.mjs)
*.key
*.key.pub
notiq-updater.key
